const register = document.querySelector('#register_button');

register.addEventListener('click', e => { e.preventDefault();
  window.location.href = '../HTML/applicationSuccess.html';
                                })
